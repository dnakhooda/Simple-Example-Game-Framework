import { EventObj } from "../../Library/EngineLibrary.js";
export class Events extends EventObj {
    onClick(event) {
        switch (event.key) {
            case "": break;
        }
        ;
    }
    ;
    offClick(event) {
        switch (event.key) {
            case "": break;
        }
        ;
    }
    ;
    mouseDown(event) {
    }
    ;
    mouseMove(event) {
    }
    ;
}
;
